# ==============================================================================
# Opsora Agent API — Environment Variables
# Copy this file to .env and fill in the real values.
# ==============================================================================

# ---- API Keys ----
# NVIDIA NIM API key (primary provider — required)
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional fallback providers
OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Opsora client API keys — comma-separated Bearer tokens.
# REQUIRED in production; never leave empty on a public deployment.
OPSORA_API_KEYS=opsk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Shell tool guard. Keep disabled in production unless explicitly required.
OPSORA_DISABLE_RUN_COMMAND=1

# ---- Server ----
HOST=0.0.0.0
PORT=8080
UPSTREAM_TIMEOUT=120

# ---- Rate Limiting ----
RATE_LIMIT_RPM=60

# ---- Database ----
DB_PATH=./opsora_usage.db
BILLING_DB_PATH=./billing.db
IDR_USD_RATE=16100

# ---- Midtrans ----
MIDTRANS_SERVER_KEY=SB-Mid-server-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Use false for production; true is sandbox only.
MIDTRANS_SANDBOX=true

# ---- CORS ----
CORS_ORIGINS=https://useopsora.com,https://www.useopsora.com,https://app.useopsora.com,https://dashboard.useopsora.com

# ---- Logging ----
LOG_LEVEL=info

# ---- NVIDIA Base URL ----
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1/
